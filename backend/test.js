import connectDB from './config/db.js'; import User from './models/User.js'; import dotenv from 'dotenv'; dotenv.config(); const run = async () => { await connectDB(); const user = await User.findOne(); if(!user) return console.log('No user'); try { user.shippingAddress = { address: '1', city: '1', postalCode: '1', country: '1' }; await user.save(); console.log('Success'); } catch(e) { console.error('Error:', e); } process.exit(); }; run();
